
server:
  port: 28083

nacosServerHost: 212.64.18.166


spring:
  application:
    name: example
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  cloud:
    nacos:
      config:
        server-addr: ${nacosServerHost}:30000
      discovery:
        server-addr: ${nacosServerHost}:30000



logging:
  level:
    com.robben: debug

